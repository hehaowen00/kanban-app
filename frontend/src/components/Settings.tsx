import "./styles/settings.css";

function SettingsView({ }: Props) {
  return (
    <div className="settings bg-grey br-3 flex flex-col">
    Hello World
    </div>
  );
}

type Props = {
};

export default SettingsView;
